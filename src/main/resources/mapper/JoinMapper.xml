<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="com.bitcamp.mapper.JoinMapper">
  	<select id="userIdCheck" parameterType="String" resultType="int">
  		select count(*) from member where user_id = #{user_id}
  	</select>
  	
  	<insert id="userJoin" parameterType="String">
  		insert into member values(member_seq.nextval, #{id}, #{password}, #{name}, null, #{email}, sysdate, 0, null, null, 0, null)
  	</insert>
  </mapper>